# REVIEW — заметки для ревью следующим агентом

Сюда пишутся вещи, которые нельзя проверить из среды разработки и которые
нужно не забыть сделать/проверить после деплоя. Пункт закрывается вычёркиванием
с пометкой (дата, что сделано).

---

## [ ] Прод: /lang в hub-боте и мелкие правки кабинета — живая проверка

**2026-08-29:** hub-бот переведён на RU/EN: команда `/lang` (кнопки RU/EN,
выбор пишется в `sellers.locale`, миграция `u5c6d7e8f9a0`, локально прогнана
up→down→up с вставкой модели). Выбор главнее языка Telegram; без выбора —
как раньше: ru* → русский, остальным английский, а неизвестный язык Telegram —
русский (у покупателей наоборот — EN; hub всегда был русским, молча
переводить существующих продавцев нельзя). Переведены интерактивные экраны
hub-бота (/start, /mybots, подключение магазина, «Администраторы») и все
фоновые пуши продавцу (заказ оплачен/завис/отзыв/сообщение из чата, выплата
ушла/не удалась, токен отозван, роль выдана/снята, канал подключён, API
события). Меню команд в чате после /lang перезаписывается на выбранном языке
(пер-чатовый scope). Роутер /lang стоит до FSM-ловушек — диалог не сбрасывает.
Тесты: pytest 304 (294 старых без правок + 10 новых), vitest 122.

Также в этом же инкременте, мелочи: админу магазина вернули зелёную карточку
баланса кассы сверху (кошелёк с выводом по-прежнему скрыт); строку
«Terms of Service and Privacy Policy» в профиле покупателя вернули с −10px
к −9px.

Проверить в проде:

1. `/lang` в hub-боте: кнопки, выбор «🇬🇧 English» → подтверждение по-английски,
   меню команд в чате перезаписывается английским; смена назад — аналогично.
2. После смены языка: `/mybots`, карточка магазина, подключение нового
   магазина — все тексты на выбранном языке.
3. Фоновые пуши продавцу на EN: оплата заказа, отзыв, сообщение из чата,
   выплата; на RU — по-прежнему русские.
4. /lang посреди FSM-диалога (например, ввода названия магазина в /newshop):
   язык меняется, диалог продолжается без сброса.
5. Продавец без выбора языка: ru* — русский, иностранный — английский;
   существующие продавцы после деплоя не должны «внезапно» стать
   англоговорящими.
6. Кабинет админа: зелёная карточка баланса сверху видна, кошелька с выводом
   нет.

---

## [ ] Прод: система ролей «Добавить администратора» — живая проверка

**2026-08-29:** владелец приглашает админа в карточке магазина («👥
Администраторы»), админ ведёт магазин наравне с владельцем, кроме денег.
Миграция `t4b5c6d7e8f9` прогнана локально up→down→up; логика покрыта тестами
(pytest 294, vitest 122), матрица прав проверена откатом (без гардов админ
получает доступ к выводу денег — тесты падают). Telegram из среды разработки
недоступен.

Проверить в проде:

1. Приглашение: владелец → /mybots → карточка → «👥 Администраторы» → «➕
   Добавить админа» → @username человека, который уже нажимал /start у
   hub-бота. Кандидат получает пуш, у него в /start появляется «🛠 Магазины,
   где я администратор», кнопка ведёт в кабинет магазина.
2. Кандидат без регистрации в Botify: владелец видит «Никого такого в Botify
   нет» (поиск по числовому ID работает даже без юзернейма).
3. Права админа в кабинете: товары, заказы, отправка, чат, отзывы, рассылки,
   имя/лого, вкл/выкл магазина — работают; кошелёк и баланс скрыты, прямые
   запросы на вывод/удаление дают 403.
4. Действие админа «Отключить магазин» приходит пушем владельцу.
5. Снятие роли: кнопка «✖️ @username» → «Убрать» — у бывшего админа пуш,
   кабинет больше не открывается (404), пункт в /start исчезает.

---

## [ ] Прод: меню-кнопка, полный экран, модерация отзывов — живая проверка

**2026-08-28:** пять доработок перед первыми пользователями (коммиты
`83e80c2`…`05cc6f9`). Логика покрыта тестами (pytest 281, vitest 114), каждая
проверена откатом правки (тесты падают без кода); миграция `r3c4d5e6f7g8`
прогнана локально up→down→up на пустой базе с вставкой модели против
мигрированной схемы. Telegram и живой Postgres из среды разработки недоступны.

Проверить в проде:

1. **Меню-кнопка бота** (пачка 3): у бота магазина кнопка меню открывает
   витрину с текстом из настроек; смена текста кнопки в кабинете обновляет
   и меню-кнопку; отключение бота возвращает обычное меню. Лимит 64 символа
   на текст обрезается на нашей стороне, но точный лимит Telegram живьём
   не проверен.
2. **Язык бота магазина** (пачка 3): у иностранного покупателя (язык Telegram
   не ru, без ручного выбора) `/start`, «я не робот», одобрение подписки и
   отказные тексты чата — на английском; ручной выбор языка в профиле
   главнее.
3. **Полный экран** (пачка 4): витрина покупателя разворачивается на весь
   экран (Telegram ≥ 8.0), кабинет продавца — нет; контент не заезжает под
   шапку на iPhone с «чёлкой». На старых клиентах всё должно выглядеть
   как раньше.
4. **Таймер оплаты** (пачка 2): у неоплаченного заказа тикает отсчёт, через
   час заказ отменяется, по ссылке из @CryptoBot платёж уже не проходит;
   «Оплатить» перезапускает срок.
5. **Модерация отзывов** (пачка 5): оценка ≤ 3 видна продавцу во вкладке
   «Отзывы» с бейджем «На проверке» и скрыта от покупателей (рейтинг товара
   и магазина не меняется); «Одобрить» публикует, «Скрыть» прячет; оценка ≥ 4
   публикуется сразу; правка оценки туда-обратно пересчитывает статус.
6. **Автопубликация отзывов**: отзывы, ожидающие дольше 7 дней
   (`review_moderation_days`), публикует фоновая задача раз в 10 минут —
   для проверки пересидеть неделю не нужно, порог можно временно уменьшить
   в переменных окружения.
7. **Рассылки**: кнопка-рупор в шапке кабинета открывает отдельный экран
   рассылок, отправка работает, история отображается.

---

## Известные ограничения — осознанные решения (не чинить без обсуждения)

**2026-08-28:** записаны из разбора перед первыми пользователями, чтобы
следующие сессии не «доделывали» их как баги.

- **Чат с продавцом до покупки невозможен.** Чат заказа открывается только
  у оплаченного заказа; вопрос «а как спросить про товар заранее» остаётся
  без ответа сознательно — первичный фильтр вопросов делаем товарами и
  описаниями, а не операционкой. Если понадобиться — это новая фича
  (отдельный чат до заказа), а не починка этой.
- **Корзина не синхронизируется между устройствами.** Корзина живёт в
  localStorage, привязанном к `telegram_id` (пачка 1): один аккаунт на
  разных устройствах видит разные корзины. Серверная корзина решило бы это,
  но стоит отдельной миграции, API и гонок синхронизации — для первого
  запуска осознанно не делаем.

---

## [ ] Прод: язык уведомлений покупателю — живая проверка

**2026-08-27:** пуши покупателю от бота магазина переведены на «как в Mini App»:
ручной выбор языка в профиле главнее всего; без него русский — только тем, у
кого язык Telegram начинается с ru; всем остальным — английский. Выбор из
Mini App уезжает заголовком `X-Locale` и пишется в `customers.locale`
(миграция `p1a2b3c4d5e6`, локально прогнана up→down→up). Затронуты: подтверждение
оплаты, «продавец отправил заказ» (+ запись в чате), обёртки «💬 Заказ #N»
в переписке. Продавецские пуши в hub и интерактивные тексты бота — по-прежнему
русские, так и задумано.

Проверить в проде:

1. Иностранный покупатель (язык Telegram не ru) получает пуши на английском:
   «Order #N paid!», «The seller shipped order #N!».
2. Русский покупатель — по-прежнему русские пуши.
3. Смена языка в профиле Mini App меняет язык следующего пуша (EN у русского,
   RU у иностранца).
4. В чате заказа запись об отправке — на языке покупателя.

---

## [ ] Прод: fulfill-форма с фото и дефолт EN — живая проверка

**2026-08-27:** форма отправки физического заказа — одно поле «трек-номер или
ссылка» плюс до трёх фото посылки (уезжают в чат заказа, покупателю — в
Telegram). Чекаут «YOUR ORDER» спрашивает только адрес и комментарий; после
Pay оплата открывается сразу, покупатель молча возвращается на витрину.
Плашка «Сделано через Botify» убрана из карточки товара и профиля продавца.
Язык Mini App: русский — только если язык Telegram-профиля начинается с ru,
у всех остальных (включая без данных) — английский всегда.

Проверить в проде:

1. Fulfill с фото: фото приходят покупателю в диалог с ботом, и в кабинете
   строка отправки показывает значение поля и число фото.
2. Фото-без-текста (только квадратики, поле пустым) проходит — покупателю
   уходят «посылка на фото ниже» и сами фото.
3. Старые заказы (легаси `tracking/url/note`) по-прежнему читаемы в кабинете.
4. У иностранного покупателя витрина и чекаут открываются сразу на английском;
   ручной выбор языка из профиля по-прежнему главнее.
5. Плашка «Сделано через Botify» на карточке товара и в профиле продавца
   больше не показывается, на витрине/в покупках/профиле — осталась внизу.

---

## [ ] Прод: имя/лого магазина и trust-строка — живая проверка

**2026-08-27:** хиро витрины (аватар, показное имя, trust-строка
«★ 4.9 · 12 продаж») и настройка из кабинета. Логика покрыта тестами
(pytest + vitest), миграция `n9d0e1f2a3b4` прогнана локально up→down→up на
SQLite; на живом Postgres её накатит деплой.

1. У существующего прода `shop_name` NULL, и это сознательно: витрина по-прежнему
   показывает @username, пока продавец сам не задаст имя в кабинете (автоподстановка
   выдала бы имя за владельца). Дефолт из Telegram-имени берётся только при
   подключении нового бота.
2. Выбор файла логотипа в вебвью Mini App и его отображение на витрине; замена
   лого должна убить старый адрес (токен не переиспользуется).
3. Trust-строка на живых данных: продажи считаются только по оплаченным статусам.

---

## [x] ~~Прод: миграция `c4d5e6f7a8b9` переиспользует id из аварии 24.08~~

**Закрыто 2026-08-26:** прогнано локально на базе в состоянии прода. Риск был
в том, что старый штамп заставит alembic *молча* пропустить миграцию — этого
не происходит: деплой падает громко и с точным текстом. Ниже — что проверено
и рунбук на случай, если всё же всплывёт.

**Контекст:** ровно этот revision id уже был у миграции, которая уехала в прод
из ветки `ox-alpha`, а потом исчезла из репозитория вместе с откатом ветки.
Прод тогда лёг с `Can't locate revision identified by 'c4d5e6f7a8b9'`; лечили
полным сбросом схемы (`DROP SCHEMA public CASCADE`), вместе с ним ушла и
таблица `alembic_version` со старым штампом.

**Проверено (Postgres 16, база в состоянии прода — ревизия `a7b8c9d0e1f2`, с
данными: продавец, магазин, 5 заказов во всех статусах, выплаты, фото товара):**

1. `alembic upgrade head` доходит до `h4a5b6c7d8e9`, все шесть новых миграций
   применяются на непустой базе.
2. Бэкфилл `orders.delivered_at`: у обоих доставленных проставлен и равен
   `paid_at`, у недоставленных остался NULL.
3. Данные целы: заказы, выплаты, товары на месте, `products.image_url`
   не пострадал.
4. Откат `downgrade a7b8c9d0e1f2` и повторный `upgrade head` — чисто, данные
   на месте (путь отката деплоя рабочий).
5. Джоб архивации чатов прогнан **на схеме, построенной alembic**, а не через
   `create_all`: 4 сообщения перенеслись, `archived_at` заполнен, порядок
   переписки и id сохранены. Это то место, где прятался баг с NOT NULL.

**Если старый штамп всё же где-то уцелел** (проверено симуляцией):
деплой падает на `d8e9f0a1b2c3` с

```
sqlalchemy.exc.ProgrammingError: (psycopg2.errors.UndefinedTable)
relation "order_chats" does not exist
```

(не `DuplicateTable`, как предполагалось раньше — alembic пропускает
пропущенную ревизию, и следующая миграция не находит её таблиц). Лечение —
одна строка, вернуть штамп на реальное состояние схемы и накатить заново:

```sql
UPDATE alembic_version SET version_num = 'a7b8c9d0e1f2';
```

затем обычный `alembic upgrade head`. Проверено: после этого цепочка доходит
до `h4a5b6c7d8e9`, все четыре таблицы чата создаются. Сносить схему не нужно.

**Чего проверить было нельзя:** содержимое `alembic_version` на живом проде —
Railway из контейнера недоступен. Отдельной сверки это не требует: неверный
штамп не проходит молча, деплой упадёт с текстом выше. Если хочется
убедиться заранее — `SELECT * FROM alembic_version;` в Railway → Postgres,
ожидается `a7b8c9d0e1f2` (или `g2b3c4d5e6f7`, если прод уже собирался с
`ox-alpha`).

**Правило прежнее:** уже применённые миграции не переименовываются и не
удаляются; любая правка — новой миграцией сверху.

---

## [x] ~~Аватар магазина из Telegram — функция убрана~~

**Закрыто 2026-08-26:** по решению владельца функция удалена целиком, а не
чинилась. Причина: бот не может прочитать собственную аватарку — `getChat` на
себя отвечает `chat not found` (сам себе бот не писал), а метода вроде
`getMyPhoto` в Bot API нет: картинка бота живёт только в @BotFather. В шапке
витрины осталась буква юзернейма — визуально прод не изменился, фото там
никогда и не появлялось.

**Удалено:** `services/bot_avatars.py`, модель `BotAvatar`, эндпоинт
`GET /api/bot-avatars/{token}`, поле `shop_avatar_url` в `/api/store/{bot_id}`,
докачка при подключении бота, ленивая докачка на витрине (вместе с ней —
гонка на уникальном `bot_id` и поход в Telegram из запроса покупателя),
`tests/test_bot_avatars.py`. Таблица снята миграцией `i5b6c7d8e9f0`; она была
пуста по той же причине — скачивание всегда падало, строк не создавалось.

**Если когда-нибудь понадобится логотип магазина:** источником должен быть не
Telegram, а загрузка продавцом в кабинете — вся инфраструктура уже есть на
фото товаров (магические байты, лимит 5 МБ, токен-адрес, `immutable`).

---

## [x] ~~Прод: тема и язык — живая проверка~~

**Закрыто 2026-08-26:** владелец проверил в проде — переключатели темы 🌙/☀️
и языка RU/EN работают, Mini App переводится на лету. Пуши бота остаются
русскими, так и задумано.

---

## [x] ~~Прод: фото в чате заказа — живая проверка доставки~~

**Закрыто 2026-08-26:** владелец проверил в проде — доставка фото работает.

**Контекст:** в relay-чате появилась отправка фото (продавец — из кабинета через
«+», покупатель — фото в диалог с ботом магазина). Логика покрыта тестами
(pytest + vitest), но доставку `send_photo` и приём фото от покупателя
проверял только деплой — реальный Telegram из среды разработки недоступен.

**Остаточное, если руки дойдут:** из четырёх сценариев списка три ломаются
заметно, а один — тихо: реплай покупателя на фото продавца должен адресовать
именно тот заказ (`_chat_id_by_reply` скоупит по паре бот+покупатель).
Ошибись он — сообщение уйдёт в соседний заказ того же покупателя, и снаружи
это выглядит как обычная переписка. На двух заказах одного покупателя
проверяется за минуту. Логика покрыта тестами
(`test_reply_addresses_exact_order_chat`, `test_reply_from_other_customer_does_not_hijack_chat`),
так что это перестраховка, а не подозрение.

---

## [ ] Ревизия MVP: отчёт и прогон

**2026-08-26:** полная ревизия перед первыми пользователями. Что осталось
починить и почему — `docs/MVP-GAPS.md`. Сценарий ручного прогона продавца и
покупателя, включая блок «сломать нарочно», — `docs/QA-checklist.md`.

Начинать сессию с них: там уже разобрано, что в коде есть, чего нет и что
из этого сознательно отложено по брифу.

---

## [ ] НАПОМИНАНИЕ: бэкапы Postgres в Railway

Висит намеренно и не закрывается само — это не задача по коду, а настройка,
которую нужно сделать руками и потом изредка перепроверять.

**Что:** Railway → Postgres → включить ежедневные бэкапы, в идеале
point-in-time recovery.

**Почему это самое дорогое в списке:** в базе лежит всё — заказы, выплаты,
переписка по заказам, отзывы. В коде бэкапов нет и быть не должно; без
настройки в Railway сбой инстанса означает потерю всего разом, и починить
это из приложения будет нечем.

**Закрывать только после того, как бэкап реально проверен восстановлением** —
включённый переключатель сам по себе ещё ничего не гарантирует.

---

## [x] ~~Детекция отозванного токена бота~~

**Закрыто 2026-08-26:** сделано (`app/services/bot_health.py`). Раз в час
`get_me` по каждому активному боту; 401 → `webhook_status = "revoked"` и один
пуш продавцу в hub со ссылкой на переподключение. Сетевые ошибки и пятисотки
Telegram статус не трогают: разовый сбой связи не повод пугать продавца.
Уведомление уходит на переходе в `revoked`, повторов на каждом тике нет.
В `/mybots` статус пишется словами («токен отозван, подключи заново»), а не
только цветом кружка. Тесты: отзыв метит и уведомляет один раз, сетевая
ошибка не метит, 401 отличается от прочих ошибок.

**Проверить в проде:** отозвать токен тестового бота в @BotFather → в течение
часа приходит пуш, в `/mybots` красный статус с текстом. Из среды разработки
это не проверить — Telegram недоступен.

---

## [x] ~~Напоминание по зависшим manual-заказам~~

**Закрыто 2026-08-26:** сделано (`app/services/order_health.py`). Отдельного
признака «ручная доставка» в схеме нет и не понадобилось: цифровые заказы
уходят в `delivered` прямо при оплате, поэтому заказ, зависший в статусе
`paid`, — это ровно случай «покупатель заплатил и ждёт продавца».

Через `stuck_order_hours` (по умолчанию 24) продавцу уходит напоминание —
одно на заказ (`orders.reminded_at`) и одним сообщением на продавца, даже
если зависших заказов несколько: пачка пушей читается как спам. Отметка
ставится до отправки, поэтому упавший пуш не превращается в вечное
напоминание на каждом тике.

**Не входит:** авто-отмена с возвратом денег — рефанд через Crypto Pay
требует отдельной проработки.

---

## [x] ~~Восстановление рассылок из статуса sending~~

**Закрыто 2026-08-26:** сделано (`app/services/mailing.py:revive_stuck_mailings`).
Рассылки без признака жизни дольше `mailing_stuck_minutes` (10) возвращаются
в `pending` и уходят следующим тиком.

**Важная деталь, которая чуть не стала багом:** по одной лишь метке старта
живую рассылку оживило бы прямо на ходу — 0.06 с на сообщение, то есть порог
в 10 минут набирается уже на ~10 тысячах покупателей, и часть базы получила бы
сообщение дважды. Поэтому `mailings.heartbeat_at` — не «когда началась», а
«когда была жива»: отправка обновляет её каждые `HEARTBEAT_EVERY` сообщений
(~12 секунд). Тест на это есть, и он падает, если отметку убрать.

Повтор для части покупателей при реальном обрыве всё же возможен: точку
обрыва мы не знаем, а получить сообщение дважды лучше, чем не получить вовсе.

---

## [x] ~~Прод: застрявшие пачки выплат с SPEND_ID_ALREADY_USED~~

**Закрыто 2026-08-25:** выполнено в проде по инструкции — пострадавшие
пачки найдены и удалены.

**Контекст:** миграция `f9e8d7c6b5a4` добавила `payout_batches.spend_id` —
случайный токен вместо `batch-{id}` (тот ломался после сброса базы: нумерация
начиналась заново, и Crypto Pay отвечал SPEND_ID_ALREADY_USED на уже занятый id).
Миграция бэкфиллит старые строки значением `batch-{id}` — это правильно для
нормальных пачек, но у пачек, которые упали **именно с этой ошибкой**, бэкфилл
возвращает им тот самый уже занятый spend_id. Такая пачка будет падать вечно:
деньги по ней не ушли, но Crypto Pay считает идемпотентность-ключ исчерпанным.

**Что сделать после деплоя (Railway → Postgres):**

1. Найти пострадавшие пачки:

```sql
SELECT id, bot_id, amount, status, last_error
FROM payout_batches
WHERE status IN ('pending', 'failed')
  AND last_error LIKE '%SPEND_ID_ALREADY_USED%';
```

2. Для каждой найденной — удалить пачку. Доли продавца отвяжутся автоматически
   (`payouts.batch_id` имеет FK `ON DELETE SET NULL`), останутся `pending`,
   и следующее нажатие «Вывести» соберёт из них новую пачку с чистым случайным
   токеном — деньги дойдут без ручных переводов:

```sql
DELETE FROM payout_batches WHERE id = <id из шага 1>;
```

3. Продавцу, если он уже видел ошибку, можно написать, что выплата уйдёт
   повторным «Вывести» — сумма на месте.

**Не делать:** не помечать такие пачки sent вручную — в этом сценарии перевод
реально не уходил, отметка «sent» спишет баланс без выплаты.

Проверено локально: миграция up/down/up на SQLite, бэкфилл даёт `batch-{id}`,
pytest 121 passed. На живом Postgres миграцию прогонит деплой.
